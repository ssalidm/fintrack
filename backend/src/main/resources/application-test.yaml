spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    clean-disabled: true
    validate-on-migrate: true
    locations: classpath:db/migration

fintrack:
  security:
    jwt:
      secret: NOSUIDDGICbMmX4Uzu4LDrp/IRMT7CSstm+cXl9UOQ4=

    mfa:
      encryption-key: ZGJrM4MSG1zQyXXeHuCN0K7a/qvQFS97FbseZOY5cr4=
      issuer: salif
      challenge-ttl: PT5M
      challenge-retention-duration: PT24H
      max-attempts: 5
      lockout-duration: PT15M

    cors:
      allowed-origins:
        - http://localhost:5173

  email:
    enabled: false

  recurring:
    processing-cron: "-"
    schedule-batch-size: 100
    max-catch-up-per-schedule: 100
